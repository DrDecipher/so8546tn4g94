using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace SPH3
{
    /// <summary>
    /// Enums for Custom Editors
    /// </summary>
    public enum TimeTypeEnum { Range, Animate, Trigger };

    public enum ShapeTypeEnum { Disk, Plane, Box, Cylinder};

    public enum ColliderTypeEnum { Plane, Box, Sphere, Cylinder };
}
